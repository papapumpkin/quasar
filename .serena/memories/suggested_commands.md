# Suggested Commands

- `go build -o quasar .` — Build the binary
- `go test ./...` — Run all tests
- `go test ./internal/loop/...` — Run loop tests
- `go vet ./...` — Run Go vet
- `go fmt ./...` — Format code
- `bd ready` / `bd list` / `bd sync` — Beads issue tracking
