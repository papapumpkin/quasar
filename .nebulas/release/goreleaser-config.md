# goreleaser-config

Create `.goreleaser.yml` at project root with cross-compilation for linux/amd64, darwin/arm64, darwin/amd64. Configure archives, checksums, and changelog grouping by conventional commit prefix. Add `dist/` to `.gitignore`.
