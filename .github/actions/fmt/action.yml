name: Format Check
description: Check Go source formatting with gofmt

runs:
  using: composite
  steps:
    - uses: actions/setup-go@v5
      with:
        go-version-file: go.mod

    - name: Run format check
      shell: bash
      run: bash scripts/fmt.sh
