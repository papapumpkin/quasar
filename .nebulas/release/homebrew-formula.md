# homebrew-formula

Create `.github/workflows/promote-homebrew.yml` (workflow_dispatch with tag input) that downloads release archives, computes SHA256 checksums, generates formula from template, and pushes to `papapumpkin/homebrew-tap`. Create `.github/homebrew/quasar.rb.tmpl` formula template.
